---

---

<header id="header">
  </header>
<section class="hero">
  <div class="hero-bg" id="heroBg"></div>
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <div class="hero-text">Cuidamos tu salud con excelencia</div>
  </div>
</section>




<style>
  :root {
    --primary-color: #0077c2;
    --secondary-color: #28a745;
    --light-green: #a6e22e;
    --apple-green: #8cc152;
    --text-light: #ffffff;
  }

  header {
    position: fixed;
    width: 100%;
    padding: 1rem;
    top: 0;
    left: 0;
    background: transparent;
    transition: background 0.3s ease-in-out;
    z-index: -100;
  }

  .scrolled {
    background: rgba(0, 0, 0, 0.8);
  }

  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .logo {
    font-size: 1.8rem;
    font-weight: bold;
    color: var(--text-light);
    text-decoration: none;
  }

  .menu {
    display: flex;
    list-style: none;
    gap: 1.5rem;
  }

  .menu li a {
    color: var(--text-light);
    text-decoration: none;
    font-size: 1.2rem;
    transition: color 0.3s;
  }

  .menu li a:hover {
    color: var(--light-green);
  }

  .hero {
    height: 100vh;
    overflow: hidden;
    position: relative;
  }

  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 110vh;
    background: url('/images/CARDIOLOGIA10.png') center/cover no-repeat;
    transform: translateY(0);
    transition: transform 0.1s ease-out;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
  }

  .hero-content {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    height: 100%;
    color: var(--text-light);
  }

  .hero-text {
    font-size: 2.5rem;
    font-weight: bold;
    text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
    opacity: 0;
    animation: fadeIn 1.5s ease-in-out forwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .menu {
      display: none;
    }

    .menu.active {
      display: flex;
      flex-direction: column;
      position: absolute;
      top: 60px;
      right: 20px;
      background: rgba(0, 0, 0, 0.9);
      padding: 1rem;
      border-radius: 8px;
    }

    .hamburger {
      display: block;
      font-size: 2rem;
      color: var(--text-light);
      cursor: pointer;
    }
  }
</style>


<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const header = document.getElementById("header");
    const menu = document.getElementById("menu");
    const hamburger = document.getElementById("hamburger");
    const heroBg = document.getElementById("heroBg");

    document.addEventListener("scroll", () => {
      if (window.scrollY > 50) {
        header.classList.add("scrolled");
      } else {
        header.classList.remove("scrolled");
      }
      
      // Efecto Parallax
      heroBg.style.transform = `translateY(${window.scrollY * 0.5}px)`;
    });

    hamburger.addEventListener("click", () => {
      menu.classList.toggle("active");
    });
  });
</script>


<!-- ---
---

<style>
  :root {
    --primary-color: #0077c2;
    --secondary-color: #28a745;
    --light-green: #a6e22e;
    --apple-green: #8cc152;
    --text-light: #ffffff;
  }

  header {
    position: fixed;
    width: 100%;
    padding: 1rem;
    top: 0;
    left: 0;
    background: transparent;
    transition: background 0.3s ease-in-out;
    z-index: 100;
  }

  .scrolled {
    background: rgba(0, 0, 0, 0.8);
  }

  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .logo {
    font-size: 1.8rem;
    font-weight: bold;
    color: var(--text-light);
    text-decoration: none;
  }

  .menu {
    display: flex;
    list-style: none;
    gap: 1.5rem;
  }

  .menu li a {
    color: var(--text-light);
    text-decoration: none;
    font-size: 1.2rem;
    transition: color 0.3s;
  }

  .menu li a:hover {
    color: var(--light-green);
  }

  .hero {
    height: 100vh;
    background: url('/images/hero-image.jpg') center/cover no-repeat;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
  }

  .hero-text {
    position: relative;
    color: var(--text-light);
    font-size: 2.5rem;
    font-weight: bold;
    text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
    transform: translateY(50px);
    opacity: 0;
    animation: fadeInUp 1.5s ease-out forwards;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .parallax {
    transform: translateY(var(--scroll));
  }

  @media (max-width: 768px) {
    .menu {
      display: none;
    }

    .menu.active {
      display: flex;
      flex-direction: column;
      position: absolute;
      top: 60px;
      right: 20px;
      background: rgba(0, 0, 0, 0.9);
      padding: 1rem;
      border-radius: 8px;
    }

    .hamburger {
      display: block;
      font-size: 2rem;
      color: var(--text-light);
      cursor: pointer;
    }
  }
</style>

<header id="header">
  <div class="nav-container">
    <a href="/" class="logo w-55"><img src="../../public/images/LogoBlueBorder.png" alt=""></a>
    <nav>
      <ul class="menu" id="menu">
        <li><a href="/especialidades">Especialidades</a></li>
        <li><a href="/servicios">Nuestros Servicios</a></li>
        <li><a href="/profesionales">Profesionales</a></li>
        <li><a href="/contacto">Contacto</a></li>
      </ul>
    </nav>
    <div id="hamburger" class="hamburger">☰</div>
  </div>
</header>

<section class="hero">
  <div class="hero-text parallax"><img src="../../public/images/9.png" alt="">Cuidamos tu salud con excelencia</div>
</section>

<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const header = document.getElementById("header");
    const menu = document.getElementById("menu");
    const hamburger = document.getElementById("hamburger");

    document.addEventListener("scroll", () => {
      if (window.scrollY > 50) {
        header.classList.add("scrolled");
      } else {
        header.classList.remove("scrolled");
      }
      document.documentElement.style.setProperty("--scroll", `${window.scrollY * 0.5}px`);
    });

    hamburger.addEventListener("click", () => {
      menu.classList.toggle("active");
    });
  });
</script>
 -->

 ---
---
